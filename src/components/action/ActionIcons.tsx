import React from 'react';
import { Icon, IconProps } from '@ui-kitten/components';

export const ActionIcons = {
  FlagIcon: (props: IconProps) => {
    return <Icon {...props} name="flag-outline" />;
  }
};
